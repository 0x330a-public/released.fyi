import { createSystem } from 'frog/ui'
import {colors} from "frog/ui";
import {loadGoogleFont} from "frog";

export const { Box, Heading, Spacer, Text, VStack, HStack, Image, vars } = createSystem({
  colors: colors.dark,
})
